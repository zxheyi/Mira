# Mira MVP 实施计划

> **给后续 Agent 工作者：** 实施本计划时必须使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`。本文件使用 checkbox 语法追踪进度。

**目标：** 构建一个本地优先的开发者 Agent 记忆中枢，支持保存会话、提炼项目记忆、搜索记忆，并通过 CLI 与 MCP Server 把相关上下文提供给 Codex、Claude Code、Cursor、OpenClaw 等编码 Agent。

**架构：** Mira 第一版采用 Node.js + TypeScript + SQLite。CLI 是人类使用入口，MCP Server 是 Agent 使用入口。记忆提炼先用确定性规则跑通闭环，后续再接入 LLM 做更智能的总结、归并和重要性判断。

**技术栈：** TypeScript、Node.js、SQLite FTS5、`better-sqlite3`、`commander`、`vitest`、`tsx`、MCP TypeScript SDK。

---

## 文件结构

- `package.json`：npm 脚本、依赖、CLI bin 入口。
- `.gitignore`：忽略本地数据库、构建产物和依赖目录。
- `tsconfig.json`：严格 TypeScript 配置。
- `src/index.ts`：CLI 入口。
- `src/db/schema.ts`：SQLite schema 与迁移。
- `src/db/client.ts`：数据库连接辅助方法。
- `src/projects/projectRoot.ts`：从当前目录自动探测项目根目录。
- `src/projects/projectStore.ts`：项目创建、查找、列表能力。
- `src/threads/threadStore.ts`：会话保存与查询能力。
- `src/workingMemory/workingMemoryStore.ts`：当前任务、偏好、近期决策等工作记忆。
- `src/memory/memoryStore.ts`：记忆写入、读取、FTS 搜索能力。
- `src/memory/kinds.ts`：MVP 支持的记忆类型枚举。
- `src/memory/distill.ts`：从会话文本中提炼第一版确定性记忆。
- `src/context/contextBundle.ts`：为 Agent 生成可消费的项目上下文包。
- `src/mcp/server.ts`：MCP Server 与工具注册。
- `src/mcp/stdio.ts`：MCP stdio 传输层启动入口。
- `tests/db/schema.test.ts`：数据库 schema 测试。
- `tests/projects/projectStore.test.ts`：项目存储测试。
- `tests/projects/projectRoot.test.ts`：项目根目录自动探测测试。
- `tests/threads/threadStore.test.ts`：会话保存测试。
- `tests/workingMemory/workingMemoryStore.test.ts`：工作记忆测试。
- `tests/memory/memoryStore.test.ts`：记忆写入与搜索测试。
- `tests/memory/distill.test.ts`：确定性记忆提炼测试。
- `tests/context/contextBundle.test.ts`：上下文包生成测试。
- `tests/mcp/server.test.ts`：MCP Server 烟雾测试。
- `tests/integration/localLoop.test.ts`：保存、提炼、搜索、输出的端到端回归测试。
- `docs/agent-config/`：Claude Code、Cursor 等 Agent 的 MCP 配置示例。
- `docs/sessions/`：人工可读的导入会话记录。

## 阶段 0：锁定产品边界

### 任务 0.1：明确 MVP 范围

**文件：**

- 修改：`README.md`
- 新建：`docs/product/mvp.md`

- [ ] **步骤 1：创建 MVP 范围文档**

创建 `docs/product/mvp.md`：

```markdown
# Mira MVP

Mira 是一个本地优先的开发者 Agent 记忆中枢，第一用户是这台机器的拥有者。

## 第一承诺

当我在 Codex、Claude Code、Cursor、OpenClaw 或其他编码 Agent 之间切换时，Agent 能恢复关键项目上下文，而不需要我反复解释。

## MVP 范围

- 保存 Agent 会话为项目关联 thread。
- 从会话中提炼长期有效的项目记忆。
- 搜索项目记忆。
- 为当前项目生成 context bundle。
- 维护 working memory，记录当前任务、近期决策和个人偏好。
- 自动从当前目录探测项目根路径，减少 `--project-root` 手动输入。
- 通过 CLI 暴露保存、提炼、搜索、上下文生成能力。
- 通过 MCP tools 暴露搜索和上下文能力。
- 使用 SQLite 本地存储，并支持 schema 版本迁移。
- 支持 Markdown / JSON 导出。
- 提供可直接接入 Agent 的 MCP stdio 启动命令和配置示例。

## 暂不做

- 多用户账号。
- 云同步。
- 计费。
- 官网或公开 Web App。
- 插件市场。
- 团队空间。
- 通用消费者个人记忆。
- 多格式会话导入适配器：Codex JSONL、Claude transcript、Cursor 导出等放到 post-MVP。
- Tag、Relation、Source 独立实体放到 post-MVP，MVP 先在 Memory / Thread 字段中保留扩展空间。
```

- [ ] **步骤 2：在 README 中链接 MVP 文档**

更新 `README.md`：

```markdown
# Mira

Personal developer-agent memory hub.

Mira is intended to let Codex, Claude Code, Cursor, OpenClaw, and other coding agents share project memory instead of rebuilding context from scratch in every session.

## Project Notes

- [MVP Scope](docs/product/mvp.md)
- [Session 019f45f0-40bf-7261-8685-d5e0a6a8bf13](docs/sessions/019f45f0-40bf-7261-8685-d5e0a6a8bf13.md)
```

- [ ] **步骤 3：验证文档可读**

运行：

```bash
sed -n '1,200p' README.md
sed -n '1,240p' docs/product/mvp.md
```

期望：两个文件都能正常打印，没有断链或占位内容。

## 阶段 1：本地项目骨架

### 任务 1.1：初始化 TypeScript CLI 项目

**文件：**

- 新建：`package.json`
- 新建：`.gitignore`
- 新建：`tsconfig.json`
- 新建：`src/index.ts`

- [ ] **步骤 1：创建 `package.json`**

关键内容：

```json
{
  "name": "mira",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "mira": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "better-sqlite3": "^11.10.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
```

- [ ] **步骤 2：创建 `.gitignore`**

创建 `.gitignore`：

```gitignore
node_modules/
dist/
.mira/
.env
.DS_Store
```

- [ ] **步骤 3：创建 `tsconfig.json`**

关键配置：

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": ".",
    "types": ["node", "vitest/globals"]
  },
  "include": ["src/**/*.ts", "tests/**/*.ts"]
}
```

- [ ] **步骤 4：创建最小 CLI 入口**

`src/index.ts` 先只实现：

```ts
#!/usr/bin/env node
import { Command } from "commander";

const program = new Command();

program
  .name("mira")
  .description("Local developer-agent memory hub")
  .version("0.1.0");

program
  .command("health")
  .description("Check that Mira is installed")
  .action(() => {
    console.log("mira:ok");
  });

program.parse();
```

- [ ] **步骤 5：安装依赖**

运行：

```bash
npm install
```

期望：生成 `package-lock.json`，依赖安装成功。

- [ ] **步骤 6：验证 CLI**

运行：

```bash
npm run dev -- health
```

期望输出：

```text
mira:ok
```

### 任务 1.2：创建 SQLite Schema

**文件：**

- 新建：`src/db/client.ts`
- 新建：`src/db/schema.ts`
- 新建：`tests/db/schema.test.ts`

补充要求：schema 必须包含 `schema_version` 表，用于后续安全升级 `.mira/mira.sqlite`。

- [ ] **步骤 1：先写失败测试**

`tests/db/schema.test.ts` 需要验证核心表存在：

- `schema_version`
- `projects`
- `threads`
- `working_memory`
- `memories`
- `memory_fts`

运行：

```bash
npm test -- tests/db/schema.test.ts
```

期望：因为 `src/db/schema.ts` 尚不存在而失败。

- [ ] **步骤 2：实现数据库连接**

`src/db/client.ts`：

```ts
import Database from "better-sqlite3";
import { mkdirSync } from "node:fs";
import { dirname } from "node:path";

export function openDatabase(path: string): Database.Database {
  if (path !== ":memory:") {
    mkdirSync(dirname(path), { recursive: true });
  }
  return new Database(path);
}
```

- [ ] **步骤 3：实现迁移**

`src/db/schema.ts` 建表：

- `schema_version`：记录当前数据库 schema 版本，MVP 初始版本为 `1`。
- `projects`：项目空间。
- `threads`：导入的 Agent 会话。
- `working_memory`：当前任务、近期决策和偏好快照。
- `memories`：提炼出的长期记忆。
- `memory_fts`：FTS5 全文搜索索引。

核心字段：

```text
schema_version: version, applied_at
projects: id, name, root_path, created_at
threads: id, project_id, title, source, raw_text, created_at
working_memory: id, project_id, kind, content, updated_at
memories: id, project_id, thread_id, kind, content, content_hash, importance, created_at
memory_fts: id, project_id, content
```

- [ ] **步骤 4：跑通测试**

运行：

```bash
npm test -- tests/db/schema.test.ts
```

期望：PASS。

## 阶段 2：核心数据闭环

### 任务 2.1：实现项目根目录自动探测

**文件：**

- 新建：`src/projects/projectRoot.ts`
- 新建：`tests/projects/projectRoot.test.ts`

- [ ] **步骤 1：写测试**

测试应覆盖：

- 从子目录向上找到最近的 `.git` 目录。
- 如果找不到 `.git`，返回传入的当前目录。

- [ ] **步骤 2：实现 `detectProjectRoot(startDir)`**

实现要求：

- 从 `startDir` 开始逐级向上查找 `.git`。
- 找到后返回该目录。
- CLI 中未传 `--project-root` 时默认使用 `detectProjectRoot(process.cwd())`。

- [ ] **步骤 3：验证**

运行：

```bash
npm test -- tests/projects/projectRoot.test.ts
```

期望：PASS。

### 任务 2.2：实现 Project Store

**文件：**

- 新建：`src/projects/projectStore.ts`
- 新建：`tests/projects/projectStore.test.ts`

- [ ] **步骤 1：写测试**

测试应覆盖：

- 创建项目。
- 按 `rootPath` 查找项目。
- 重复创建同一路径时返回已有项目。

测试数据：

```ts
{
  name: "Mira",
  rootPath: "/Users/limaolin/Desktop/Mira"
}
```

- [ ] **步骤 2：实现类型**

`Project` 类型：

```ts
export type Project = {
  id: string;
  name: string;
  rootPath: string;
  createdAt: string;
};
```

- [ ] **步骤 3：实现方法**

需要实现：

```ts
createProject(db, { name, rootPath })
findProjectByRoot(db, rootPath)
```

实现要求：

- 使用 `randomUUID()` 生成 `id`。
- 使用 ISO 时间字符串作为 `createdAt`。
- 对外暴露 camelCase，对 SQLite 使用 snake_case。

- [ ] **步骤 4：验证**

运行：

```bash
npm test -- tests/projects/projectStore.test.ts
```

期望：PASS。

### 任务 2.3：实现 Thread Import

**文件：**

- 新建：`src/threads/threadStore.ts`
- 新建：`tests/threads/threadStore.test.ts`

- [ ] **步骤 1：写测试**

测试应保存这个真实会话 ID：

```text
019f45f0-40bf-7261-8685-d5e0a6a8bf13
```

测试内容应包含：

```text
Mira should be a personal developer-agent memory hub.
```

- [ ] **步骤 2：实现类型**

`Thread` 类型：

```ts
export type Thread = {
  id: string;
  projectId: string;
  title: string;
  source: string;
  rawText: string;
  createdAt: string;
};
```

- [ ] **步骤 3：实现保存方法**

需要实现：

```ts
saveThread(db, { id, projectId, title, source, rawText })
```

实现要求：

- `id` 可以外部传入，方便保存 Codex thread id。
- 同一个 `id` 再次保存时更新标题、来源和原文。
- `thread.projectId` 必须关联到已有项目。

- [ ] **步骤 4：验证**

运行：

```bash
npm test -- tests/threads/threadStore.test.ts
```

期望：PASS。

### 任务 2.4：实现 Memory Store 与搜索

**文件：**

- 新建：`src/memory/kinds.ts`
- 新建：`src/memory/memoryStore.ts`
- 新建：`tests/memory/memoryStore.test.ts`

- [ ] **步骤 1：写搜索测试**

测试应写入这条记忆：

```text
Use SQLite plus FTS before adding vector search.
```

然后用查询：

```text
SQLite vector
```

期望能搜到该记忆。

- [ ] **步骤 2：实现类型**

`Memory` 类型：

```ts
export type Memory = {
  id: string;
  projectId: string;
  threadId: string | null;
  kind: MemoryKind;
  content: string;
  contentHash: string;
  importance: number;
  createdAt: string;
};
```

- [ ] **步骤 3：定义 MVP 记忆类型**

`src/memory/kinds.ts`：

```ts
export const memoryKinds = [
  "decision",
  "convention",
  "architecture",
  "preference",
  "task",
  "fact"
] as const;

export type MemoryKind = (typeof memoryKinds)[number];
```

- [ ] **步骤 4：实现写入与搜索**

需要实现：

```ts
addMemory(db, { projectId, threadId, kind, content, importance })
searchMemories(db, { projectId, query, limit })
```

实现要求：

- `addMemory` 同时写入 `memories` 和 `memory_fts`。
- 使用 `content_hash` 做幂等写入，同一 `projectId + threadId + kind + content_hash` 不重复插入。
- `distill` 同一 thread 前，允许先清理该 thread 旧记忆，保证重复执行不污染数据。
- `searchMemories` 只搜索当前项目。
- 结果按 `importance desc, created_at desc` 排序。

- [ ] **步骤 5：验证**

运行：

```bash
npm test -- tests/memory/memoryStore.test.ts
```

期望：PASS。

## 阶段 3：工作记忆、记忆提炼与上下文包

### 任务 3.0：实现 Working Memory

**文件：**

- 新建：`src/workingMemory/workingMemoryStore.ts`
- 新建：`tests/workingMemory/workingMemoryStore.test.ts`

- [ ] **步骤 1：写测试**

测试应覆盖写入和读取这些 kind：

- `current_task`：当前正在做什么。
- `recent_decision`：近期决策。
- `preference`：用户偏好。

- [ ] **步骤 2：实现 store**

需要实现：

```ts
setWorkingMemory(db, { projectId, kind, content })
listWorkingMemory(db, { projectId })
```

实现要求：

- 同一个 `projectId + kind` 保留最新一条。
- `updated_at` 每次写入时刷新。
- context bundle 必须优先输出 working memory，再输出长期 memory。

- [ ] **步骤 3：验证**

运行：

```bash
npm test -- tests/workingMemory/workingMemoryStore.test.ts
```

期望：PASS。

### 任务 3.1：实现规则版记忆提炼

**文件：**

- 新建：`src/memory/distill.ts`
- 新建：`tests/memory/distill.test.ts`

- [ ] **步骤 1：写测试**

输入：

```text
Product name: Mira.
Core positioning: personal agent memory infrastructure for developer workflows.
Recommended storage direction: SQLite plus FTS first.
```

期望输出 3 条 `decision` 类型记忆，`importance` 都为 `5`。

- [ ] **步骤 2：实现提炼规则**

第一版只识别这些稳定前缀：

```ts
const durablePrefixes = [
  "Product name:",
  "Core positioning:",
  "First-stage goal:",
  "Initial scope:",
  "Recommended storage direction:",
  "Useful Rowboat inspiration:",
  "Deferred scope:"
];
```

`distillMemories(rawText)` 返回：

```ts
export type DistilledMemory = {
  kind: "decision";
  content: string;
  importance: number;
};
```

- [ ] **步骤 3：验证**

运行：

```bash
npm test -- tests/memory/distill.test.ts
```

期望：PASS。

### 任务 3.2：实现 Context Bundle

**文件：**

- 新建：`src/context/contextBundle.ts`
- 新建：`tests/context/contextBundle.test.ts`

- [ ] **步骤 1：写测试**

写入记忆：

```text
First-stage goal: avoid re-explaining context when switching agents.
```

生成 bundle 后应包含：

```text
# Mira Context Bundle
avoid re-explaining context
```

- [ ] **步骤 2：实现 bundle 生成**

需要实现：

```ts
buildContextBundle(db, { projectId, limit, maxTokens })
```

输出格式：

```markdown
# Mira Context Bundle

- [decision] First-stage goal: avoid re-explaining context when switching agents.
```

实现要求：

- 先输出 working memory，再输出长期 memory。
- 只取当前项目记忆。
- 按 `importance desc, created_at desc` 排序。
- 支持可选 `maxTokens`，MVP 可用 `Math.ceil(text.length / 4)` 粗略估算并截断。
- 输出 Markdown，便于复制到任意 Agent 会话。

- [ ] **步骤 3：验证**

运行：

```bash
npm test -- tests/context/contextBundle.test.ts
```

期望：PASS。

## 阶段 4：CLI 命令

### 任务 4.1：加入数据库初始化命令

**文件：**

- 修改：`src/index.ts`
- 新建：`tests/cli-smoke.test.ts`

- [ ] **步骤 1：写 CLI 烟雾测试**

测试：

```bash
npx tsx src/index.ts health
```

期望：

```text
mira:ok
```

需要安装：

```bash
npm install --save-dev execa
```

- [ ] **步骤 2：在 CLI 中加入 `--db` 与 `init`**

CLI 应支持：

```bash
npm run dev -- --db .mira/test.sqlite init
```

期望：

```text
initialized:.mira/test.sqlite
```

实现要求：

- 默认数据库路径：`.mira/mira.sqlite`
- `init` 命令调用 `openDatabase()` 和 `migrate()`。
- 初始化后关闭数据库连接。

- [ ] **步骤 3：验证**

运行：

```bash
npm test -- tests/cli-smoke.test.ts
npm run dev -- --db .mira/test.sqlite init
```

期望：测试通过，数据库文件被创建。

### 任务 4.2：加入核心 CLI 命令

所有命令都应支持省略 `--project-root`：未显式传入时，使用 `detectProjectRoot(process.cwd())` 自动推断。

**文件：**

- 修改：`src/index.ts`
- 可按需要新增：`src/cli/*.ts`

- [ ] **步骤 1：实现项目初始化命令**

命令：

```bash
npm run dev -- project add --name Mira --root /Users/limaolin/Desktop/Mira
```

期望：

```text
project:saved:Mira
```

- [ ] **步骤 2：实现会话保存命令**

命令：

```bash
npm run dev -- thread save \
  --id 019f45f0-40bf-7261-8685-d5e0a6a8bf13 \
  --project-root /Users/limaolin/Desktop/Mira \
  --title "分析 Rowboat 仿 Mem 可行性" \
  --source codex \
  --file docs/sessions/019f45f0-40bf-7261-8685-d5e0a6a8bf13.md
```

期望：

```text
thread:saved:019f45f0-40bf-7261-8685-d5e0a6a8bf13
```

- [ ] **步骤 3：实现记忆提炼命令**

命令：

```bash
npm run dev -- memory distill --thread 019f45f0-40bf-7261-8685-d5e0a6a8bf13
```

期望至少包含：

```text
memory:created
```

- [ ] **步骤 4：实现记忆搜索命令**

命令：

```bash
npm run dev -- memory search "SQLite FTS vector"
```

期望能看到：

```text
Recommended storage direction: SQLite plus FTS first
```

- [ ] **步骤 5：实现 working memory 命令**

命令：

```bash
npm run dev -- working set --kind current_task --content "实现 Mira MVP 数据闭环"
npm run dev -- working list
```

期望输出包含：

```text
current_task: 实现 Mira MVP 数据闭环
```

- [ ] **步骤 6：实现上下文包命令**

命令：

```bash
npm run dev -- context bundle --project-root /Users/limaolin/Desktop/Mira
```

期望输出 Markdown，并包含：

```text
# Mira Context Bundle
```

## 阶段 5：MCP Agent 接口

### 任务 5.1：创建 MCP Server

**文件：**

- 新建：`src/mcp/server.ts`
- 新建：`tests/mcp/server.test.ts`

- [ ] **步骤 1：写 MCP 烟雾测试**

测试目标：

- `createMcpServer()` 可以创建 server。
- server 名称是 `mira`。
- 版本是 `0.1.0`。

- [ ] **步骤 2：实现 server factory**

`src/mcp/server.ts`：

```ts
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function createMcpServer(): McpServer {
  return new McpServer({
    name: "mira",
    version: "0.1.0"
  });
}
```

- [ ] **步骤 3：验证**

运行：

```bash
npm test -- tests/mcp/server.test.ts
```

期望：PASS。

### 任务 5.2：加入 MCP stdio 启动入口

**文件：**

- 新建：`src/mcp/stdio.ts`
- 修改：`src/index.ts`
- 新建：`docs/agent-config/claude-code.md`
- 新建：`docs/agent-config/cursor.md`

- [ ] **步骤 1：实现 stdio transport**

`src/mcp/stdio.ts` 负责创建 server 并连接 stdio transport。使用 `StdioServerTransport`，不要只创建 server factory。

- [ ] **步骤 2：实现 CLI 命令**

命令：

```bash
npm run dev -- mcp serve --db .mira/dev.sqlite
```

期望：进程保持运行，等待 MCP client 通过 stdio 通信。

- [ ] **步骤 3：写 Agent 配置示例**

`docs/agent-config/claude-code.md` 和 `docs/agent-config/cursor.md` 至少包含：

```json
{
  "mcpServers": {
    "mira": {
      "command": "node",
      "args": ["/Users/limaolin/Desktop/Mira/dist/index.js", "mcp", "serve"]
    }
  }
}
```

### 任务 5.3：注册 Agent 可用工具

**文件：**

- 修改：`src/mcp/server.ts`
- 新建或修改：`tests/mcp/server.test.ts`

- [ ] **步骤 1：注册 `search_memory`**

输入：

```json
{
  "projectRoot": "/Users/limaolin/Desktop/Mira",
  "query": "SQLite FTS vector",
  "limit": 5
}
```

输出：匹配的记忆列表。

- [ ] **步骤 2：注册 `set_working_memory` 和 `list_working_memory`**

`set_working_memory` 输入：

```json
{
  "projectRoot": "/Users/limaolin/Desktop/Mira",
  "kind": "current_task",
  "content": "实现 Mira MVP 数据闭环"
}
```

`list_working_memory` 输入：

```json
{
  "projectRoot": "/Users/limaolin/Desktop/Mira"
}
```

输出：当前项目的 working memory 列表。

- [ ] **步骤 3：注册 `get_context_bundle`**

输入：

```json
{
  "projectRoot": "/Users/limaolin/Desktop/Mira",
  "limit": 20
}
```

输出：Markdown context bundle。

- [ ] **步骤 4：验证 MCP Server**

运行：

```bash
npm test -- tests/mcp/server.test.ts
```

期望：PASS。

## 阶段 6：导入第一条真实会话与端到端测试

### 任务 6.1：导入 Rowboat / Nowledge 规划会话

**文件：**

- 使用：`docs/sessions/019f45f0-40bf-7261-8685-d5e0a6a8bf13.md`
- 写入：本地 SQLite 数据库。

- [ ] **步骤 1：初始化数据库**

运行：

```bash
npm run dev -- --db .mira/dev.sqlite init
```

期望：

```text
initialized:.mira/dev.sqlite
```

- [ ] **步骤 2：保存项目**

运行：

```bash
npm run dev -- --db .mira/dev.sqlite project add \
  --name Mira \
  --root /Users/limaolin/Desktop/Mira
```

期望：

```text
project:saved:Mira
```

- [ ] **步骤 3：保存会话**

运行：

```bash
npm run dev -- --db .mira/dev.sqlite thread save \
  --id 019f45f0-40bf-7261-8685-d5e0a6a8bf13 \
  --project-root /Users/limaolin/Desktop/Mira \
  --title "分析 Rowboat 仿 Mem 可行性" \
  --source codex \
  --file docs/sessions/019f45f0-40bf-7261-8685-d5e0a6a8bf13.md
```

期望：

```text
thread:saved:019f45f0-40bf-7261-8685-d5e0a6a8bf13
```

- [ ] **步骤 4：提炼记忆**

运行：

```bash
npm run dev -- --db .mira/dev.sqlite memory distill \
  --thread 019f45f0-40bf-7261-8685-d5e0a6a8bf13
```

期望：

```text
memory:created
```

- [ ] **步骤 5：搜索记忆**

运行：

```bash
npm run dev -- --db .mira/dev.sqlite memory search "SQLite FTS vector"
```

期望包含：

```text
Recommended storage direction: SQLite plus FTS first
```

- [ ] **步骤 6：生成上下文包**

运行：

```bash
npm run dev -- --db .mira/dev.sqlite context bundle \
  --project-root /Users/limaolin/Desktop/Mira
```

期望包含：

```text
# Mira Context Bundle
```

### 任务 6.2：补充端到端集成测试

**文件：**

- 新建：`tests/integration/localLoop.test.ts`

- [ ] **步骤 1：覆盖核心闭环**

测试流程：

1. 创建临时 SQLite 数据库。
2. 创建项目。
3. 保存 thread。
4. distill 生成 memory。
5. search 找回 memory。
6. build context bundle 输出 working memory 和长期 memory。

- [ ] **步骤 2：验证**

运行：

```bash
npm test -- tests/integration/localLoop.test.ts
```

期望：PASS。

## Post-MVP 明确延后项

- `Tag`、`Relation`、`Source` 独立实体：MVP 先保留字段扩展空间，不做完整图谱。
- 多格式导入适配器：MVP 先支持 Markdown / raw text，Codex JSONL、Claude transcript、Cursor 导出后续用 adapter 模式接入。
- LLM 智能提炼：MVP 先使用确定性规则，后续再加入模型提炼、合并和置信度评分。

## 总体验收门槛

- [ ] `npm test` 通过。
- [ ] `npm run build` 通过。
- [ ] `.gitignore` 忽略 `.mira/`、`node_modules/`、`dist/`。
- [ ] `npm run dev -- health` 输出 `mira:ok`。
- [ ] `npm run dev -- --db .mira/dev.sqlite init` 可以创建本地数据库。
- [ ] 会话 `019f45f0-40bf-7261-8685-d5e0a6a8bf13` 能作为第一条真实 thread 导入。
- [ ] 搜索能返回该会话提炼出的项目决策。
- [ ] working memory 能在 context bundle 中优先出现。
- [ ] `npm run dev -- mcp serve` 可以作为 MCP stdio server 启动。
- [ ] MCP tools 覆盖 `search_memory`、`set_working_memory`、`list_working_memory`、`get_context_bundle`。
- [ ] Agent 配置示例能说明 Claude Code / Cursor 如何接入 Mira。
- [ ] context bundle 是可读 Markdown，并且足够短，可以直接贴进新的 Agent 会话。

## 推荐执行顺序

1. 阶段 0：锁定 MVP 文档。
2. 阶段 1：初始化 TypeScript 项目、`.gitignore` 和数据库 schema。
3. 阶段 2：实现项目探测、project、thread、memory 三个核心 store。
4. 阶段 3：实现 working memory、规则版记忆提炼和 context bundle。
5. 阶段 4：把核心能力暴露为 CLI 命令。
6. 阶段 5：把搜索和上下文能力暴露为可启动的 MCP stdio server。
7. 阶段 6：导入现有规划会话并补充端到端测试，形成第一条真实项目记忆。

## 自检

- 覆盖范围：本计划覆盖本地存储、schema 版本、项目空间、自动项目探测、会话保存、working memory、记忆提炼、搜索、上下文包、CLI、MCP stdio 启动和 Agent 配置示例。云同步、UI、多用户账号、通用个人记忆不在 MVP 范围内。
- 占位扫描：没有保留 `TBD`、`TODO` 或“以后实现”式占位任务。
- 类型一致性：对外 TypeScript API 使用 camelCase，SQLite 字段使用 snake_case。`Project`、`Thread`、`Memory` 的字段名在后续任务中保持一致。
